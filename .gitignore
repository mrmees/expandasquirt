# Arduino build artifacts
build/
*.elf
*.hex
*.bin

# Secrets — never commit
secrets.h

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Project-local arduino-cli config (we use the user sketchbook globally)
arduino-cli.local.yaml
.arduino/
