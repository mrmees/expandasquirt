# Arduino build artifacts
build/
*.elf
*.hex
*.bin

# Secrets — never commit
secrets.h

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Project-local arduino-cli config (we use the user sketchbook globally)
arduino-cli.local.yaml
.arduino/

# Host-side test build artifacts
tests/test_runner
tests/test_runner.exe

# Session handoff doc — transient, not part of production source
HANDOFF.md
