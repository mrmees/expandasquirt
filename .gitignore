# Arduino build artifacts
build/
*.elf
*.hex
*.bin

# Secrets — never commit
secrets.h
prototypes/**/arduino_secrets.h

# Prototype-local upstream source caches (research scratch)
prototypes/**/.tmp/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*~

# Project-local arduino-cli config (we use the user sketchbook globally)
arduino-cli.local.yaml
.arduino/

# Host-side test build artifacts
tests/test_runner
tests/test_runner.exe

# Session handoff doc — transient, not part of production source
HANDOFF.md

# Brainstorm/visual companion artifacts (transient session content)
.superpowers/

# Android Studio / Gradle
local.properties
app/build/
.gradle/
*.iml
.idea/
.cxx/
