# EXPANDASQUIRT v4 — pinned library versions
# Update this file whenever bumping a dependency.
#
# Libraries are installed in the user Arduino sketchbook
# (default: ~/Documents/Arduino/libraries/, OneDrive-redirected on this machine).
# `arduino-cli lib install <name>` from anywhere installs to the sketchbook.

arduino:renesas_uno        @ 1.5.3
ArduinoBLE                 @ 2.0.1
Arduino_LED_Matrix         @ bundled with renesas_uno (no separate install)
autowp/arduino-mcp2515     @ 1.3.1   (installed from https://github.com/autowp/arduino-mcp2515.git)
OTAUpdate                  @ bundled with renesas_uno   (legacy — Arduino-supplied, prefer ArduinoOTA below for v4.x wireless updates)
WiFiS3                     @ bundled with renesas_uno

# v4.x additions
ArduinoOTA                 @ 1.1.1   (JAndrassy fork — supports R4 WiFi via WiFiS3 + InternalStorageRenesas; install via `arduino-cli lib install ArduinoOTA@1.1.1`)
